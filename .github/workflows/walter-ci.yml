name: Walter CI
'on':
  push:
    branches: '*'
jobs:
  expose-secrets:
    runs-on: ubuntu-latest
    steps:
      - run: echo "What is my email?"
      - env:
          MY_SECRET: ${{ secrets.WALTER_GIT_EMAIL }}
        run: echo ${{secrets.WALTER_GIT_EMAIL}} | sed 's/./& /g'
